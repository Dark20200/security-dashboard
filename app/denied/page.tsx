export default function DeniedPage() {
  return (
    <div className="flex flex-col items-center justify-center min-h-screen bg-black text-red-400 text-xl">
      🚫 Access Denied — You are not authorized to view this page.
    </div>
  );
}
